# csv-formula-injection-test (almost never reported)

> CSV formula injection that does not reach other users' data / cannot execute is not written by default. Unauthorized export goes through `idor-test.md`.
