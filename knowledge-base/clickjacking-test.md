# clickjacking-test (no security headers — do not report)

> Clickjacking with missing security headers is **not reported**. If the click chain reaches bind-change / payment / authorization, report it under that write endpoint, not via this doc.
