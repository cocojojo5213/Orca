# email-header-injection-test (almost never reported)

> Email header injection that does not take over an account / change a password is not written by default. For reset chains use `authbypass-test.md` + `http-host-header-test.md`.
