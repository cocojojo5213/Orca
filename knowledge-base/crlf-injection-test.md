# crlf-injection-test (almost never reported)

> Pure CRLF header injection is not written by default. Host poisoning reset goes through `http-host-header-test.md`. For Ghost Bits email/smuggling use the formula `chr((k<<8)|T)`, see `ghost-bits-cast-test.md`.
