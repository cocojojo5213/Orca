# 短表迭代（进/补 `knowledge-base/quick-hits.md` 的唯一入口）

本文件是原 `~/.grok/rules/hunt-iter.md` 的自包含本地版。

- 只增强、不限制；禁止删行、禁止把「几乎不交」的行扩回长文。
- 改表前必须先读本文件现稿（磁盘），禁止用会话旧副本。
- 新增行必须四列齐全：认什么（可识别特征）/ 打哪（不登录或带会话的打法）/
  出什么算成（差分证据）/ 假点（对照失败条件）。
- 新手法来源：本场打成功的链（子代理交付必须含迭代）、知识库模块新发现。
- 已收成一行的手法是共识，不扩写。